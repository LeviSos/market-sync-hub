INSERT INTO public.items (slug,name,rarity,weapon,category,base_price) VALUES
('dual-berettas-heist','Dual Berettas | Heist','consumer','Dual Berettas','Pistols',0.06),
('aug-contractor','AUG | Contractor','consumer','AUG','Rifles',0.14),
('famas-palm','FAMAS | Palm','consumer','FAMAS','Rifles',0.16),
('ssg-08-green-ceramic','SSG 08 | Green Ceramic','consumer','SSG 08','Rifles',0.15),
('mag-7-storm','MAG-7 | Storm','consumer','MAG-7','Heavy',0.1),
('mp9-dizzy','MP9 | Dizzy','consumer','MP9','SMGs',0.13),
('p250-boreal-forest','P250 | Boreal Forest','consumer','P250','Pistols',0.16),
('scar-20-storm','SCAR-20 | Storm','consumer','SCAR-20','Rifles',0.15),
('pp-bizon-wood-block-camo','PP-Bizon | Wood Block Camo','consumer','PP-Bizon','SMGs',0.04),
('m249-jungle','M249 | Jungle','consumer','M249','Heavy',0.05),
('famas-vendetta','FAMAS | Vendetta','industrial','FAMAS','Rifles',0.54),
('ssg-08-carbon-fiber','SSG 08 | Carbon Fiber','industrial','SSG 08','Rifles',0.09),
('pp-bizon-traitor','PP-Bizon | Traitor','industrial','PP-Bizon','SMGs',0.08),
('p2000-marsh','P2000 | Marsh','industrial','P2000','Pistols',0.16),
('tec-9-rust-leaf','Tec-9 | Rust Leaf','industrial','Tec-9','Pistols',0.48),
('famas-calicamo','FAMAS | CaliCamo','industrial','FAMAS','Rifles',0.18),
('p90-wash-me','P90 | Wash me','industrial','P90','SMGs',0.6),
('p2000-granite-marbleized','P2000 | Granite Marbleized','industrial','P2000','Pistols',0.53),
('mac-10-palm','MAC-10 | Palm','industrial','MAC-10','SMGs',0.13),
('mac-10-silver','MAC-10 | Silver','industrial','MAC-10','SMGs',0.09),
('ump-45-oscillator','UMP-45 | Oscillator','milspec','UMP-45','SMGs',2.55),
('ump-45-metal-flowers','UMP-45 | Metal Flowers','milspec','UMP-45','SMGs',0.43),
('xm1014-irezumi','XM1014 | Irezumi','milspec','XM1014','Heavy',1.66),
('mp9-spy-prototype','MP9 | Spy Prototype','milspec','MP9','SMGs',0.41),
('xm1014-varicamo-blue','XM1014 | VariCamo Blue','milspec','XM1014','Heavy',1.26),
('mp9-modest-threat','MP9 | Modest Threat','milspec','MP9','SMGs',0.44),
('tec-9-cracked-opal','Tec-9 | Cracked Opal','milspec','Tec-9','Pistols',0.72),
('cz75-auto-twist','CZ75-Auto | Twist','milspec','CZ75-Auto','Pistols',0.47),
('famas-2a2f','FAMAS | 2A2F','milspec','FAMAS','Rifles',2.2),
('scar-20-wild-berry','SCAR-20 | Wild Berry','milspec','SCAR-20','Rifles',0.49),
('r8-revolver-survivalist','R8 Revolver | Survivalist','milspec','R8 Revolver','Pistols',1.39),
('sawed-off-origami','Sawed-Off | Origami','milspec','Sawed-Off','Heavy',1.35),
('mp9-dark-age','MP9 | Dark Age','milspec','MP9','SMGs',1.56),
('mp5-sd-focus','MP5-SD | Focus','milspec','MP5-SD','SMGs',0.8),
('xm1014-teclu-burner','XM1014 | Teclu Burner','restricted','XM1014','Heavy',4.81),
('dual-berettas-demolition','Dual Berettas | Demolition','restricted','Dual Berettas','Pistols',1.27),
('mp7-amberline','MP7 | Amberline','restricted','MP7','SMGs',6.65),
('cz75-auto-slalom','CZ75-Auto | Slalom','restricted','CZ75-Auto','Pistols',1.51),
('pp-bizon-blue-streak','PP-Bizon | Blue Streak','restricted','PP-Bizon','SMGs',5.51),
('mp7-fade','MP7 | Fade','restricted','MP7','SMGs',8.72),
('five-seven-heat-treated','Five-SeveN | Heat Treated','restricted','Five-SeveN','Pistols',5.13),
('nova-baroque-orange','Nova | Baroque Orange','restricted','Nova','Heavy',2.11),
('cz75-auto-tread-plate','CZ75-Auto | Tread Plate','restricted','CZ75-Auto','Pistols',9.9),
('p90-chopper','P90 | Chopper','restricted','P90','SMGs',2.32),
('glock-18-umbral-rabbit','Glock-18 | Umbral Rabbit','restricted','Glock-18','Pistols',1.22),
('cz75-auto-tigris','CZ75-Auto | Tigris','restricted','CZ75-Auto','Pistols',9.31),
('desert-eagle-firebreathing','Desert Eagle | Firebreathing','restricted','Desert Eagle','Pistols',6.54),
('mag-7-praetorian','MAG-7 | Praetorian','restricted','MAG-7','Heavy',1.64),
('ak-47-redline','AK-47 | Redline','classified','AK-47','Rifles',59.06),
('aug-lil-pig','AUG | Lil'' Pig','classified','AUG','Rifles',5.75),
('m4a1-s-master-piece','M4A1-S | Master Piece','classified','M4A1-S','Rifles',5.55),
('nova-hyper-beast','Nova | Hyper Beast','classified','Nova','Heavy',9.77),
('awp-green-energy','AWP | Green Energy','classified','AWP','Rifles',57.66),
('usp-s-serum','USP-S | Serum','classified','USP-S','Pistols',5.63),
('ump-45-primal-saber','UMP-45 | Primal Saber','classified','UMP-45','SMGs',24.9),
('ump-45-wild-child','UMP-45 | Wild Child','classified','UMP-45','SMGs',4.66),
('negev-mj-lnir','Negev | Mjölnir','classified','Negev','Heavy',7.73),
('mp7-abyssal-apparition','MP7 | Abyssal Apparition','classified','MP7','SMGs',20.95),
('p90-nostalgia','P90 | Nostalgia','classified','P90','SMGs',59.92),
('desert-eagle-starcade','Desert Eagle | Starcade','classified','Desert Eagle','Pistols',39.37),
('p90-cold-blooded','P90 | Cold Blooded','classified','P90','SMGs',9.81),
('sawed-off-devourer','Sawed-Off | Devourer','classified','Sawed-Off','Heavy',4.06),
('m4a4-buzz-kill','M4A4 | Buzz Kill','covert','M4A4','Rifles',185.16),
('shadow-daggers-forest-ddpat','★ Shadow Daggers | Forest DDPAT','covert','Shadow Daggers','Knives',30.33),
('shadow-daggers-night','★ Shadow Daggers | Night','covert','Shadow Daggers','Knives',16.42),
('navaja-knife-boreal-forest','★ Navaja Knife | Boreal Forest','covert','Navaja Knife','Knives',30.07),
('survival-knife-slaughter','★ Survival Knife | Slaughter','covert','Survival Knife','Knives',23.32),
('survival-knife-tiger-tooth','★ Survival Knife | Tiger Tooth','covert','Survival Knife','Knives',35.51),
('navaja-knife-crimson-web','★ Navaja Knife | Crimson Web','covert','Navaja Knife','Knives',205.12),
('famas-bad-trip','FAMAS | Bad Trip','covert','FAMAS','Rifles',117.83),
('paracord-knife-urban-masked','★ Paracord Knife | Urban Masked','covert','Paracord Knife','Knives',35.62),
('karambit-forest-ddpat','★ Karambit | Forest DDPAT','covert','Karambit','Knives',134.76),
('flip-knife-rust-coat','★ Flip Knife | Rust Coat','covert','Flip Knife','Knives',16.26),
('falchion-knife-slaughter','★ Falchion Knife | Slaughter','covert','Falchion Knife','Knives',30.08),
('talon-knife-scorched','★ Talon Knife | Scorched','covert','Talon Knife','Knives',287.22),
('huntsman-knife-bright-water','★ Huntsman Knife | Bright Water','covert','Huntsman Knife','Knives',48.09),
('flip-knife','★ Flip Knife','covert','Flip Knife','Knives',165.28),
('butterfly-knife-doppler','★ Butterfly Knife | Doppler','covert','Butterfly Knife','Knives',46.71),
('sport-gloves-scarlet-shamagh','★ Sport Gloves | Scarlet Shamagh','exotic','Sport Gloves','Gloves',856.54),
('hand-wraps-arboreal','★ Hand Wraps | Arboreal','exotic','Hand Wraps','Gloves',244.03),
('moto-gloves-eclipse','★ Moto Gloves | Eclipse','exotic','Moto Gloves','Gloves',148.8),
('sport-gloves-arid','★ Sport Gloves | Arid','exotic','Sport Gloves','Gloves',581.07),
('hand-wraps-leather','★ Hand Wraps | Leather','exotic','Hand Wraps','Gloves',469.22),
('bloodhound-gloves-guerrilla','★ Bloodhound Gloves | Guerrilla','exotic','Bloodhound Gloves','Gloves',158.98),
('hand-wraps-cobalt-skulls','★ Hand Wraps | Cobalt Skulls','exotic','Hand Wraps','Gloves',111.57),
('sport-gloves-violet-beadwork','★ Sport Gloves | Violet Beadwork','exotic','Sport Gloves','Gloves',788.5),
('sport-gloves-blaze','★ Sport Gloves | Blaze','exotic','Sport Gloves','Gloves',1881.63),
('driver-gloves-queen-jaguar','★ Driver Gloves | Queen Jaguar','exotic','Driver Gloves','Gloves',563.76),
('driver-gloves-racing-green','★ Driver Gloves | Racing Green','exotic','Driver Gloves','Gloves',120.19),
('sport-gloves-hedge-maze','★ Sport Gloves | Hedge Maze','exotic','Sport Gloves','Gloves',859.14),
('m4a4-howl','M4A4 | Howl','contraband','M4A4','Rifles',2238.49)
ON CONFLICT (slug) DO NOTHING;

INSERT INTO public.cases (slug,name,category,price,tag,is_active) VALUES
('starter-case','Starter Case','Budget',0.75,'NEW',true),
('street-ops','Street Ops Case','Budget',1.99,NULL,true),
('neon-riot','Neon Riot Case','Covered',4.99,'HOT',true),
('covert-vault','Covert Vault','Covered',14.99,NULL,true),
('blade-runner','Blade Runner','Knives',34.99,NULL,true),
('golden-hands','Golden Hands','Gloves',59.99,NULL,true),
('high-roller','High Roller','Custom',249.99,'VIP',true)
ON CONFLICT (slug) DO NOTHING;

INSERT INTO public.case_items (case_id,item_id,weight)
SELECT c.id,i.id,v.w FROM (VALUES
('starter-case','mag-7-storm',900),('starter-case','famas-palm',900),('starter-case','mp9-dizzy',900),('starter-case','p250-boreal-forest',900),('starter-case','dual-berettas-heist',900),('starter-case','famas-calicamo',600),('starter-case','p90-wash-me',600),('starter-case','famas-vendetta',600),('starter-case','mac-10-palm',600),('starter-case','pp-bizon-traitor',600),('starter-case','mp9-dark-age',360),('starter-case','xm1014-irezumi',360),('starter-case','r8-revolver-survivalist',360),('starter-case','tec-9-cracked-opal',360),('starter-case','mp9-spy-prototype',360),('starter-case','desert-eagle-firebreathing',110),('starter-case','pp-bizon-blue-streak',110),('starter-case','dual-berettas-demolition',110),('starter-case','mp7-amberline',110),('starter-case','cz75-auto-tigris',110),('starter-case','nova-hyper-beast',26),('starter-case','mp7-abyssal-apparition',26),('starter-case','p90-cold-blooded',26),('starter-case','negev-mj-lnir',26),('starter-case','awp-green-energy',26),('starter-case','survival-knife-slaughter',4),('starter-case','talon-knife-scorched',4),('starter-case','m4a4-buzz-kill',4),('starter-case','butterfly-knife-doppler',4),('starter-case','flip-knife-rust-coat',4),
('street-ops','pp-bizon-traitor',760),('street-ops','mac-10-silver',760),('street-ops','famas-calicamo',760),('street-ops','p2000-granite-marbleized',760),('street-ops','p90-wash-me',760),('street-ops','famas-2a2f',720),('street-ops','ump-45-metal-flowers',720),('street-ops','xm1014-irezumi',720),('street-ops','mp9-dark-age',720),('street-ops','xm1014-varicamo-blue',720),('street-ops','xm1014-teclu-burner',380),('street-ops','mp7-fade',380),('street-ops','mp7-amberline',380),('street-ops','dual-berettas-demolition',380),('street-ops','cz75-auto-slalom',380),('street-ops','ump-45-wild-child',110),('street-ops','awp-green-energy',110),('street-ops','ump-45-primal-saber',110),('street-ops','usp-s-serum',110),('street-ops','p90-nostalgia',110),('street-ops','navaja-knife-boreal-forest',28),('street-ops','m4a4-buzz-kill',28),('street-ops','talon-knife-scorched',28),('street-ops','butterfly-knife-doppler',28),('street-ops','navaja-knife-crimson-web',28),('street-ops','hand-wraps-leather',2),('street-ops','sport-gloves-violet-beadwork',2),('street-ops','hand-wraps-arboreal',2),('street-ops','sport-gloves-arid',2),('street-ops','moto-gloves-eclipse',2),
('neon-riot','ump-45-metal-flowers',840),('neon-riot','famas-2a2f',840),('neon-riot','ump-45-oscillator',840),('neon-riot','tec-9-cracked-opal',840),('neon-riot','xm1014-irezumi',840),('neon-riot','pp-bizon-blue-streak',700),('neon-riot','mp7-fade',700),('neon-riot','dual-berettas-demolition',700),('neon-riot','five-seven-heat-treated',700),('neon-riot','nova-baroque-orange',700),('neon-riot','ump-45-wild-child',320),('neon-riot','aug-lil-pig',320),('neon-riot','ump-45-primal-saber',320),('neon-riot','mp7-abyssal-apparition',320),('neon-riot','negev-mj-lnir',320),('neon-riot','navaja-knife-boreal-forest',130),('neon-riot','m4a4-buzz-kill',130),('neon-riot','karambit-forest-ddpat',130),('neon-riot','falchion-knife-slaughter',130),('neon-riot','survival-knife-tiger-tooth',130),('neon-riot','hand-wraps-leather',10),('neon-riot','bloodhound-gloves-guerrilla',10),('neon-riot','sport-gloves-blaze',10),('neon-riot','driver-gloves-queen-jaguar',10),('neon-riot','moto-gloves-eclipse',10),
('covert-vault','pp-bizon-blue-streak',780),('covert-vault','mp7-fade',780),('covert-vault','cz75-auto-tread-plate',780),('covert-vault','cz75-auto-slalom',780),('covert-vault','mag-7-praetorian',780),('covert-vault','negev-mj-lnir',680),('covert-vault','m4a1-s-master-piece',680),('covert-vault','mp7-abyssal-apparition',680),('covert-vault','p90-cold-blooded',680),('covert-vault','ak-47-redline',680),('covert-vault','paracord-knife-urban-masked',460),('covert-vault','shadow-daggers-forest-ddpat',460),('covert-vault','karambit-forest-ddpat',460),('covert-vault','flip-knife',460),('covert-vault','falchion-knife-slaughter',460),('covert-vault','sport-gloves-hedge-maze',80),('covert-vault','sport-gloves-blaze',80),('covert-vault','moto-gloves-eclipse',80),('covert-vault','hand-wraps-arboreal',80),('covert-vault','driver-gloves-queen-jaguar',80),
('blade-runner','ak-47-redline',720),('blade-runner','sawed-off-devourer',720),('blade-runner','p90-cold-blooded',720),('blade-runner','negev-mj-lnir',720),('blade-runner','awp-green-energy',720),('blade-runner','navaja-knife-boreal-forest',880),('blade-runner','huntsman-knife-bright-water',880),('blade-runner','navaja-knife-crimson-web',880),('blade-runner','paracord-knife-urban-masked',880),('blade-runner','karambit-forest-ddpat',880),('blade-runner','bloodhound-gloves-guerrilla',400),('blade-runner','hand-wraps-cobalt-skulls',400),('blade-runner','hand-wraps-leather',400),('blade-runner','moto-gloves-eclipse',400),('blade-runner','sport-gloves-violet-beadwork',400),
('golden-hands','flip-knife',1040),('golden-hands','huntsman-knife-bright-water',1040),('golden-hands','falchion-knife-slaughter',1040),('golden-hands','survival-knife-tiger-tooth',1040),('golden-hands','shadow-daggers-night',1040),('golden-hands','sport-gloves-arid',960),('golden-hands','driver-gloves-racing-green',960),('golden-hands','moto-gloves-eclipse',960),('golden-hands','sport-gloves-scarlet-shamagh',960),('golden-hands','sport-gloves-hedge-maze',960),
('high-roller','navaja-knife-boreal-forest',800),('high-roller','famas-bad-trip',800),('high-roller','huntsman-knife-bright-water',800),('high-roller','flip-knife-rust-coat',800),('high-roller','flip-knife',800),('high-roller','sport-gloves-hedge-maze',1180),('high-roller','sport-gloves-arid',1180),('high-roller','sport-gloves-blaze',1180),('high-roller','driver-gloves-queen-jaguar',1180),('high-roller','sport-gloves-scarlet-shamagh',1180),('high-roller','m4a4-howl',100)
) AS v(cs,it,w) JOIN public.cases c ON c.slug=v.cs JOIN public.items i ON i.slug=v.it
ON CONFLICT DO NOTHING;

CREATE TYPE public.battle_mode AS ENUM ('ffa','team2v2','crazy');
CREATE TYPE public.battle_status AS ENUM ('waiting','running','finished','cancelled');

CREATE TABLE public.upgrades (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  mode text NOT NULL DEFAULT 'balance',
  stake numeric(16,2) NOT NULL DEFAULT 0,
  target_item_id uuid REFERENCES public.items(id),
  target_value numeric(16,2) NOT NULL DEFAULT 0,
  chance numeric(8,5) NOT NULL DEFAULT 0,
  roll numeric(18,15) NOT NULL DEFAULT 0,
  won boolean NOT NULL DEFAULT false,
  server_seed_hash text,
  client_seed text,
  nonce bigint,
  created_at timestamptz NOT NULL DEFAULT now()
);
GRANT SELECT ON public.upgrades TO authenticated;
GRANT ALL ON public.upgrades TO service_role;
ALTER TABLE public.upgrades ENABLE ROW LEVEL SECURITY;

CREATE TABLE public.contracts (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  input_count int NOT NULL DEFAULT 0,
  input_value numeric(16,2) NOT NULL DEFAULT 0,
  output_item_id uuid REFERENCES public.items(id),
  output_value numeric(16,2) NOT NULL DEFAULT 0,
  roll numeric(18,15) NOT NULL DEFAULT 0,
  server_seed_hash text,
  client_seed text,
  nonce bigint,
  created_at timestamptz NOT NULL DEFAULT now()
);
GRANT SELECT ON public.contracts TO authenticated;
GRANT ALL ON public.contracts TO service_role;
ALTER TABLE public.contracts ENABLE ROW LEVEL SECURITY;

CREATE TABLE public.battles (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  mode public.battle_mode NOT NULL DEFAULT 'ffa',
  status public.battle_status NOT NULL DEFAULT 'waiting',
  player_count int NOT NULL DEFAULT 2,
  rounds int NOT NULL DEFAULT 1,
  cost numeric(16,2) NOT NULL DEFAULT 0,
  pot numeric(16,2) NOT NULL DEFAULT 0,
  created_by uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  server_seed_hash text,
  client_seed text,
  winner_slots int[] NOT NULL DEFAULT '{}',
  created_at timestamptz NOT NULL DEFAULT now(),
  started_at timestamptz,
  finished_at timestamptz
);
GRANT SELECT ON public.battles TO anon, authenticated;
GRANT ALL ON public.battles TO service_role;
ALTER TABLE public.battles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "battles public read" ON public.battles FOR SELECT TO anon, authenticated USING (true);

CREATE TABLE public.battle_cases (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  battle_id uuid NOT NULL REFERENCES public.battles(id) ON DELETE CASCADE,
  case_id uuid NOT NULL REFERENCES public.cases(id),
  position int NOT NULL
);
GRANT SELECT ON public.battle_cases TO anon, authenticated;
GRANT ALL ON public.battle_cases TO service_role;
ALTER TABLE public.battle_cases ENABLE ROW LEVEL SECURITY;
CREATE POLICY "battle cases public read" ON public.battle_cases FOR SELECT TO anon, authenticated USING (true);

CREATE TABLE public.battle_players (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  battle_id uuid NOT NULL REFERENCES public.battles(id) ON DELETE CASCADE,
  slot int NOT NULL,
  team int NOT NULL DEFAULT 0,
  user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  username text NOT NULL DEFAULT 'Bot',
  avatar_url text,
  is_bot boolean NOT NULL DEFAULT false,
  total_value numeric(16,2) NOT NULL DEFAULT 0,
  created_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (battle_id, slot)
);
GRANT SELECT ON public.battle_players TO anon, authenticated;
GRANT ALL ON public.battle_players TO service_role;
ALTER TABLE public.battle_players ENABLE ROW LEVEL SECURITY;
CREATE POLICY "battle players public read" ON public.battle_players FOR SELECT TO anon, authenticated USING (true);

CREATE TABLE public.battle_rounds (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  battle_id uuid NOT NULL REFERENCES public.battles(id) ON DELETE CASCADE,
  round int NOT NULL,
  slot int NOT NULL,
  case_id uuid REFERENCES public.cases(id),
  item_id uuid REFERENCES public.items(id),
  value numeric(16,2) NOT NULL DEFAULT 0,
  roll numeric(18,15) NOT NULL DEFAULT 0,
  created_at timestamptz NOT NULL DEFAULT now()
);
GRANT SELECT ON public.battle_rounds TO anon, authenticated;
GRANT ALL ON public.battle_rounds TO service_role;
ALTER TABLE public.battle_rounds ENABLE ROW LEVEL SECURITY;
CREATE POLICY "battle rounds public read" ON public.battle_rounds FOR SELECT TO anon, authenticated USING (true);

ALTER PUBLICATION supabase_realtime ADD TABLE public.battles;
ALTER PUBLICATION supabase_realtime ADD TABLE public.battle_players;
ALTER PUBLICATION supabase_realtime ADD TABLE public.battle_rounds;

CREATE OR REPLACE FUNCTION public.fn_upgrade(
  p_user uuid, p_inventory uuid[], p_stake numeric, p_target uuid,
  p_chance numeric, p_roll numeric, p_won boolean,
  p_hash text, p_client_seed text, p_nonce bigint
) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $$
DECLARE
  v_prof public.profiles%ROWTYPE;
  v_stake numeric(16,2) := 0;
  v_target_value numeric(16,2);
  v_tx uuid := gen_random_uuid();
  v_inv uuid;
  v_up uuid;
BEGIN
  SELECT * INTO v_prof FROM public.profiles WHERE id = p_user FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'profile_not_found'; END IF;
  IF v_prof.is_banned THEN RAISE EXCEPTION 'account_banned'; END IF;

  SELECT COALESCE(price_override, base_price) INTO v_target_value FROM public.items WHERE id = p_target AND is_active;
  IF v_target_value IS NULL THEN RAISE EXCEPTION 'target_not_found'; END IF;

  IF p_inventory IS NULL OR array_length(p_inventory,1) IS NULL THEN
    v_stake := p_stake;
    IF v_stake <= 0 THEN RAISE EXCEPTION 'invalid_stake'; END IF;
    IF v_prof.balance < v_stake THEN RAISE EXCEPTION 'insufficient_funds'; END IF;
    UPDATE public.profiles SET balance = balance - v_stake, updated_at = now() WHERE id = p_user;
  ELSE
    UPDATE public.inventory_items SET status = 'used'
     WHERE id = ANY(p_inventory) AND user_id = p_user AND status = 'owned';
    SELECT COALESCE(SUM(value),0) INTO v_stake FROM public.inventory_items
      WHERE id = ANY(p_inventory) AND user_id = p_user AND status = 'used';
    IF v_stake <= 0 THEN RAISE EXCEPTION 'items_not_available'; END IF;
  END IF;

  INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,ref_id,memo)
  VALUES (v_tx,p_user,'user_balance','debit',v_stake,'upgrade',p_target,'Upgrade stake'),
         (v_tx,NULL,'house_balance','credit',v_stake,'upgrade',p_target,'Upgrade stake');

  UPDATE public.profiles SET xp = xp + GREATEST(1, FLOOR(v_stake)::int) WHERE id = p_user;
  UPDATE public.profiles SET level = 1 + FLOOR(SQRT(xp::numeric / 25))::int WHERE id = p_user;

  IF p_won THEN
    INSERT INTO public.inventory_items (user_id,item_id,value,source,source_ref)
    VALUES (p_user,p_target,v_target_value,'upgrade',p_target) RETURNING id INTO v_inv;
    INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,ref_id,memo)
    VALUES (gen_random_uuid(),NULL,'house_balance','debit',v_target_value,'upgrade_win',p_target,'Upgrade won');
  END IF;

  INSERT INTO public.upgrades (user_id,mode,stake,target_item_id,target_value,chance,roll,won,server_seed_hash,client_seed,nonce)
  VALUES (p_user, CASE WHEN p_inventory IS NULL OR array_length(p_inventory,1) IS NULL THEN 'balance' ELSE 'items' END,
          v_stake,p_target,v_target_value,p_chance,p_roll,p_won,p_hash,p_client_seed,p_nonce)
  RETURNING id INTO v_up;

  UPDATE public.user_seeds SET nonce = p_nonce WHERE user_id = p_user AND is_active;

  RETURN jsonb_build_object('upgrade_id',v_up,'won',p_won,'inventory_id',v_inv,'stake',v_stake,
    'target_value',v_target_value,'balance',(SELECT balance FROM public.profiles WHERE id = p_user));
END;
$$;

CREATE OR REPLACE FUNCTION public.fn_contract(
  p_user uuid, p_inventory uuid[], p_item uuid, p_roll numeric,
  p_hash text, p_client_seed text, p_nonce bigint
) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $$
DECLARE
  v_prof public.profiles%ROWTYPE;
  v_in numeric(16,2);
  v_cnt int;
  v_out numeric(16,2);
  v_tx uuid := gen_random_uuid();
  v_inv uuid;
  v_c uuid;
BEGIN
  SELECT * INTO v_prof FROM public.profiles WHERE id = p_user FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'profile_not_found'; END IF;
  IF v_prof.is_banned THEN RAISE EXCEPTION 'account_banned'; END IF;
  IF array_length(p_inventory,1) IS NULL OR array_length(p_inventory,1) < 3 THEN RAISE EXCEPTION 'need_three_items'; END IF;

  UPDATE public.inventory_items SET status = 'used'
   WHERE id = ANY(p_inventory) AND user_id = p_user AND status = 'owned';
  SELECT COALESCE(SUM(value),0), COUNT(*) INTO v_in, v_cnt FROM public.inventory_items
   WHERE id = ANY(p_inventory) AND user_id = p_user AND status = 'used';
  IF v_cnt < 3 THEN RAISE EXCEPTION 'items_not_available'; END IF;

  SELECT COALESCE(price_override, base_price) INTO v_out FROM public.items WHERE id = p_item;
  IF v_out IS NULL THEN RAISE EXCEPTION 'item_not_found'; END IF;

  INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,ref_id,memo)
  VALUES (v_tx,p_user,'user_balance','debit',v_in,'contract',p_item,'Contract input'),
         (v_tx,NULL,'house_balance','credit',v_in,'contract',p_item,'Contract input'),
         (gen_random_uuid(),NULL,'house_balance','debit',v_out,'contract_out',p_item,'Contract output');

  INSERT INTO public.inventory_items (user_id,item_id,value,source,source_ref)
  VALUES (p_user,p_item,v_out,'contract',p_item) RETURNING id INTO v_inv;

  INSERT INTO public.contracts (user_id,input_count,input_value,output_item_id,output_value,roll,server_seed_hash,client_seed,nonce)
  VALUES (p_user,v_cnt,v_in,p_item,v_out,p_roll,p_hash,p_client_seed,p_nonce) RETURNING id INTO v_c;

  UPDATE public.user_seeds SET nonce = p_nonce WHERE user_id = p_user AND is_active;

  RETURN jsonb_build_object('contract_id',v_c,'inventory_id',v_inv,'input_value',v_in,'output_value',v_out);
END;
$$;

CREATE OR REPLACE FUNCTION public.fn_battle_create(
  p_user uuid, p_mode public.battle_mode, p_players int, p_cases uuid[], p_hash text, p_client_seed text
) RETURNS uuid LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $$
DECLARE
  v_prof public.profiles%ROWTYPE;
  v_cost numeric(16,2) := 0;
  v_battle uuid;
  v_id uuid;
  i int := 0;
BEGIN
  SELECT * INTO v_prof FROM public.profiles WHERE id = p_user FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'profile_not_found'; END IF;
  IF v_prof.is_banned THEN RAISE EXCEPTION 'account_banned'; END IF;
  IF array_length(p_cases,1) IS NULL THEN RAISE EXCEPTION 'no_cases'; END IF;

  FOREACH v_id IN ARRAY p_cases LOOP
    v_cost := v_cost + (SELECT price FROM public.cases WHERE id = v_id AND is_active);
  END LOOP;
  IF v_cost IS NULL OR v_cost <= 0 THEN RAISE EXCEPTION 'invalid_cases'; END IF;
  IF v_prof.balance < v_cost THEN RAISE EXCEPTION 'insufficient_funds'; END IF;

  UPDATE public.profiles SET balance = balance - v_cost, updated_at = now() WHERE id = p_user;
  INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,memo)
  VALUES (gen_random_uuid(),p_user,'user_balance','debit',v_cost,'battle_buyin','Battle entry');

  INSERT INTO public.battles (mode,status,player_count,rounds,cost,pot,created_by,server_seed_hash,client_seed)
  VALUES (p_mode,'waiting',p_players,array_length(p_cases,1),v_cost,v_cost,p_user,p_hash,p_client_seed)
  RETURNING id INTO v_battle;

  FOREACH v_id IN ARRAY p_cases LOOP
    INSERT INTO public.battle_cases (battle_id,case_id,position) VALUES (v_battle,v_id,i);
    i := i + 1;
  END LOOP;

  INSERT INTO public.battle_players (battle_id,slot,team,user_id,username,avatar_url,is_bot)
  VALUES (v_battle,0,0,p_user,v_prof.username,v_prof.avatar_url,false);

  RETURN v_battle;
END;
$$;

CREATE OR REPLACE FUNCTION public.fn_battle_join(
  p_user uuid, p_battle uuid, p_slot int, p_bot boolean
) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $$
DECLARE
  v_b public.battles%ROWTYPE;
  v_prof public.profiles%ROWTYPE;
  v_team int;
BEGIN
  SELECT * INTO v_b FROM public.battles WHERE id = p_battle FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'battle_not_found'; END IF;
  IF v_b.status <> 'waiting' THEN RAISE EXCEPTION 'battle_closed'; END IF;
  IF p_slot < 0 OR p_slot >= v_b.player_count THEN RAISE EXCEPTION 'bad_slot'; END IF;
  IF EXISTS (SELECT 1 FROM public.battle_players WHERE battle_id = p_battle AND slot = p_slot) THEN
    RAISE EXCEPTION 'slot_taken';
  END IF;

  v_team := CASE WHEN v_b.mode = 'team2v2' THEN p_slot % 2 ELSE 0 END;

  IF p_bot THEN
    IF v_b.created_by <> p_user THEN RAISE EXCEPTION 'not_owner'; END IF;
    INSERT INTO public.battle_players (battle_id,slot,team,user_id,username,is_bot)
    VALUES (p_battle,p_slot,v_team,NULL,'Bot ' || (p_slot+1),true);
  ELSE
    IF EXISTS (SELECT 1 FROM public.battle_players WHERE battle_id = p_battle AND user_id = p_user) THEN
      RAISE EXCEPTION 'already_joined';
    END IF;
    SELECT * INTO v_prof FROM public.profiles WHERE id = p_user FOR UPDATE;
    IF v_prof.is_banned THEN RAISE EXCEPTION 'account_banned'; END IF;
    IF v_prof.balance < v_b.cost THEN RAISE EXCEPTION 'insufficient_funds'; END IF;
    UPDATE public.profiles SET balance = balance - v_b.cost, updated_at = now() WHERE id = p_user;
    INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,ref_id,memo)
    VALUES (gen_random_uuid(),p_user,'user_balance','debit',v_b.cost,'battle_buyin',p_battle,'Battle entry');
    UPDATE public.battles SET pot = pot + v_b.cost WHERE id = p_battle;
    INSERT INTO public.battle_players (battle_id,slot,team,user_id,username,avatar_url,is_bot)
    VALUES (p_battle,p_slot,v_team,p_user,v_prof.username,v_prof.avatar_url,false);
  END IF;

  RETURN jsonb_build_object('filled',(SELECT COUNT(*) FROM public.battle_players WHERE battle_id = p_battle),
    'player_count',v_b.player_count);
END;
$$;

CREATE OR REPLACE FUNCTION public.fn_battle_settle(
  p_battle uuid, p_rounds jsonb, p_winner_slots int[]
) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $$
DECLARE
  v_b public.battles%ROWTYPE;
  r jsonb;
  v_winners uuid[];
  v_uid uuid;
  idx int := 0;
  v_total numeric(16,2) := 0;
BEGIN
  SELECT * INTO v_b FROM public.battles WHERE id = p_battle FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'battle_not_found'; END IF;
  IF v_b.status = 'finished' THEN RETURN jsonb_build_object('already',true); END IF;

  FOR r IN SELECT * FROM jsonb_array_elements(p_rounds) LOOP
    INSERT INTO public.battle_rounds (battle_id,round,slot,case_id,item_id,value,roll)
    VALUES (p_battle,(r->>'round')::int,(r->>'slot')::int,(r->>'case_id')::uuid,(r->>'item_id')::uuid,
            (r->>'value')::numeric,(r->>'roll')::numeric);
  END LOOP;

  UPDATE public.battle_players bp SET total_value = COALESCE((
    SELECT SUM(value) FROM public.battle_rounds br WHERE br.battle_id = p_battle AND br.slot = bp.slot),0)
  WHERE bp.battle_id = p_battle;

  SELECT array_agg(user_id) INTO v_winners FROM public.battle_players
   WHERE battle_id = p_battle AND slot = ANY(p_winner_slots) AND user_id IS NOT NULL;

  IF v_winners IS NOT NULL AND array_length(v_winners,1) > 0 THEN
    FOR r IN SELECT to_jsonb(br) FROM public.battle_rounds br WHERE br.battle_id = p_battle ORDER BY br.round, br.slot LOOP
      v_uid := v_winners[(idx % array_length(v_winners,1)) + 1];
      INSERT INTO public.inventory_items (user_id,item_id,value,source,source_ref)
      VALUES (v_uid,(r->>'item_id')::uuid,(r->>'value')::numeric,'battle',p_battle);
      v_total := v_total + (r->>'value')::numeric;
      idx := idx + 1;
    END LOOP;
    INSERT INTO public.ledger_entries (tx_id,user_id,account,direction,amount,ref_type,ref_id,memo)
    VALUES (gen_random_uuid(),NULL,'house_balance','debit',v_total,'battle_payout',p_battle,'Battle payout');
  END IF;

  UPDATE public.battles SET status = 'finished', winner_slots = p_winner_slots, finished_at = now()
   WHERE id = p_battle;

  RETURN jsonb_build_object('total',v_total,'winners',p_winner_slots);
END;
$$;

REVOKE EXECUTE ON FUNCTION public.fn_upgrade(uuid,uuid[],numeric,uuid,numeric,numeric,boolean,text,text,bigint) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION public.fn_contract(uuid,uuid[],uuid,numeric,text,text,bigint) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION public.fn_battle_create(uuid,public.battle_mode,int,uuid[],text,text) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION public.fn_battle_join(uuid,uuid,int,boolean) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION public.fn_battle_settle(uuid,jsonb,int[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.fn_upgrade(uuid,uuid[],numeric,uuid,numeric,numeric,boolean,text,text,bigint) TO service_role;
GRANT EXECUTE ON FUNCTION public.fn_contract(uuid,uuid[],uuid,numeric,text,text,bigint) TO service_role;
GRANT EXECUTE ON FUNCTION public.fn_battle_create(uuid,public.battle_mode,int,uuid[],text,text) TO service_role;
GRANT EXECUTE ON FUNCTION public.fn_battle_join(uuid,uuid,int,boolean) TO service_role;
GRANT EXECUTE ON FUNCTION public.fn_battle_settle(uuid,jsonb,int[]) TO service_role;

CREATE SCHEMA IF NOT EXISTS private;
GRANT USAGE ON SCHEMA private TO authenticated, service_role;

CREATE OR REPLACE FUNCTION private.has_role(_user_id uuid, _role public.app_role)
RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT EXISTS (SELECT 1 FROM public.user_roles WHERE user_id = _user_id AND role = _role);
$$;

CREATE OR REPLACE FUNCTION private.is_staff(_user_id uuid)
RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT EXISTS (SELECT 1 FROM public.user_roles WHERE user_id = _user_id);
$$;

REVOKE ALL ON FUNCTION private.has_role(uuid, public.app_role) FROM PUBLIC;
REVOKE ALL ON FUNCTION private.is_staff(uuid) FROM PUBLIC;
GRANT EXECUTE ON FUNCTION private.has_role(uuid, public.app_role) TO authenticated, service_role;
GRANT EXECUTE ON FUNCTION private.is_staff(uuid) TO authenticated, service_role;

DROP POLICY "own profile" ON public.profiles;
CREATE POLICY "own profile" ON public.profiles FOR SELECT TO authenticated USING (id = auth.uid() OR private.is_staff(auth.uid()));

DROP POLICY "own roles" ON public.user_roles;
CREATE POLICY "own roles" ON public.user_roles FOR SELECT TO authenticated USING (user_id = auth.uid() OR private.is_staff(auth.uid()));

DROP POLICY "own inventory" ON public.inventory_items;
CREATE POLICY "own inventory" ON public.inventory_items FOR SELECT TO authenticated USING (user_id = auth.uid() OR private.is_staff(auth.uid()));

DROP POLICY "own ledger" ON public.ledger_entries;
CREATE POLICY "own ledger" ON public.ledger_entries FOR SELECT TO authenticated USING (user_id = auth.uid() OR private.is_staff(auth.uid()));

DROP POLICY "staff read audit" ON public.admin_audit_log;
CREATE POLICY "staff read audit" ON public.admin_audit_log FOR SELECT TO authenticated USING (private.is_staff(auth.uid()));

CREATE POLICY "own upgrades" ON public.upgrades FOR SELECT TO authenticated USING (user_id = auth.uid() OR private.is_staff(auth.uid()));

CREATE POLICY "own contracts" ON public.contracts FOR SELECT TO authenticated USING (user_id = auth.uid() OR private.is_staff(auth.uid()));

DROP FUNCTION public.has_role(uuid, public.app_role);
DROP FUNCTION public.is_staff(uuid);

CREATE TABLE public.chat_messages (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  username text NOT NULL DEFAULT 'Player',
  avatar_url text,
  body text NOT NULL CHECK (char_length(btrim(body)) BETWEEN 1 AND 300),
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL DEFAULT now()
);
GRANT SELECT ON public.chat_messages TO anon, authenticated;
GRANT INSERT ON public.chat_messages TO authenticated;
GRANT ALL ON public.chat_messages TO service_role;
ALTER TABLE public.chat_messages ENABLE ROW LEVEL SECURITY;

CREATE POLICY "chat public read" ON public.chat_messages FOR SELECT TO anon, authenticated USING (true);

CREATE POLICY "chat insert own" ON public.chat_messages FOR INSERT TO authenticated
  WITH CHECK (
    user_id = auth.uid()
    AND is_deleted = false
    AND EXISTS (SELECT 1 FROM public.profiles p WHERE p.id = auth.uid() AND NOT p.is_muted AND NOT p.is_banned)
  );

CREATE POLICY "chat staff moderate" ON public.chat_messages FOR UPDATE TO authenticated
  USING (private.is_staff(auth.uid())) WITH CHECK (private.is_staff(auth.uid()));

CREATE INDEX chat_messages_recent_idx ON public.chat_messages (created_at DESC);
ALTER PUBLICATION supabase_realtime ADD TABLE public.chat_messages;

CREATE TABLE public.battle_seeds (
  battle_id uuid PRIMARY KEY REFERENCES public.battles(id) ON DELETE CASCADE,
  server_seed text NOT NULL,
  client_seed text NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);
GRANT ALL ON public.battle_seeds TO service_role;
ALTER TABLE public.battle_seeds ENABLE ROW LEVEL SECURITY;