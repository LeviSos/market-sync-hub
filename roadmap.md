# Tasks

- [x] Restore the uploaded project source into this workspace.
- [x] Fix the Steam sign-in callback so a missing Steam profile never blocks sign-in.
- [x] Stop CMS queries from running before the session is confirmed (no false "session expired").
- [x] MarketCSGO sync: public fallback without a key, private endpoints with a key, 5 req/s limit.
- [x] Sync button: spinner, disabled state, table refetch, localized success toast (EN/RU/UA).
- [ ] Connect the existing Supabase database to this project (user action: Project Settings → Connectors → Supabase).
- [ ] Save Steam trade links locally per account with relaxed validation and clear notifications.
- [ ] Handle seed reveal failures without crashing; label fallback seeds as demo values.
- [ ] Show the admin menu only for a verified admin and preserve the protected /admin page.
